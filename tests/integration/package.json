{
  "name": "linkedin-integration-tests",
  "version": "1.0.0",
  "scripts": {
    "preflight": "node scripts/preflight.js",
    "test": "jest --runInBand --forceExit --testTimeout=30000",
    "test:integration": "npm run preflight && npm test"
  },
  "dependencies": {
    "node-fetch": "^2.7.0",
    "form-data": "^4.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
